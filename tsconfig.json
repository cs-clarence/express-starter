{
  "extends": "./tsconfig/tsconfig.node.json",
  "exclude": ["dist"],

  "include": [
    "src/**/*.ts",
    "src/**/*.mts",
    "src/**/*.cts",
    "src/**/*.js",
    "src/**/*.cjs",
    "src/**/*.mjs",
    "src/**/*.json"
  ],

  "compilerOptions": {
    /* Modules */
    "rootDir": "./src",
    "paths": {
      "$/*": ["./src/*"]
    },
    /* Modules */

    /* Emit */
    "outDir": "./dist",
    "declarationDir": "./dist"
    /* Emit */
  }
}
